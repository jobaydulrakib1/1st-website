{
  "name": "project",
  "version": "1.0.0",
  "description": "A full-stack web application for video content management with AI features and real-time collaboration.",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && node server.js",
    "test": "jest",
    "build": "cd frontend && npm run build"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "socket.io": "^4.6.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
